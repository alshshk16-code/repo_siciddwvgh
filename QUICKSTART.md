# 🚀 دليل البدء السريع - رُمْح شودان

## التثبيت في 3 خطوات

### 1. تثبيت المكتبات

```bash
cd /home/ubuntu/shodans_spear
./install.sh
```

أو يدوياً:

```bash
pip3 install -r requirements.txt
```

### 2. إعداد مفتاح Shodan API

احصل على مفتاح مجاني من: https://account.shodan.io/

**الطريقة الأولى (موصى بها):**

```bash
export SHODAN_API_KEY="YOUR_API_KEY_HERE"
```

**الطريقة الثانية:**

عدّل ملف `config.py`:

```python
SHODAN_API_KEY = "YOUR_API_KEY_HERE"
```

### 3. تشغيل الأداة

```bash
python3 spear.py
```

---

## الاستخدام الأساسي

### البحث عن كاميرات ويب

1. شغّل الأداة: `python3 spear.py`
2. اختر `[1]` - الصيد السريع
3. اختر `[1]` - كاميرات ويب
4. أدخل عدد النتائج (مثال: `10`)
5. اختر `y` لمهاجمتها فوراً

### البحث عن قواعد بيانات مفتوحة

1. شغّل الأداة
2. اختر `[1]` - الصيد السريع
3. اختر `[2]` - قواعد بيانات MongoDB
4. اختر `y` لمهاجمتها

### عرض الغنائم

1. من القائمة الرئيسية
2. اختر `[5]` - عرض الغنائم
3. ستظهر جميع الأهداف المخترقة مع:
   - عناوين IP
   - أسماء المستخدمين وكلمات المرور
   - روابط الوصول المباشر

### تصدير النتائج

1. اختر `[7]` - تصدير البيانات
2. اختر التنسيق:
   - `[1]` JSON (جميع البيانات)
   - `[2]` تقرير نصي (قابل للقراءة)
3. ستجد الملف في مجلد `data/`

---

## استعلامات مخصصة مفيدة

### كاميرات في دولة معينة

```
webcamxp country:US
```

### خوادم SSH بكلمات مرور افتراضية

```
"default password" port:22
```

### قواعد بيانات MongoDB في أوروبا

```
"MongoDB Server Information" port:27017 country:DE
```

### أجهزة توجيه MikroTik

```
"RouterOS" port:8291
```

---

## نصائح سريعة

✅ **ابدأ بأعداد صغيرة** (10-20 نتيجة) لتجربة الأداة

✅ **استخدم VPN** لإخفاء هويتك

✅ **راجع الإحصائيات** بانتظام لمتابعة تقدمك

✅ **صدّر البيانات** بانتظام لحفظ نسخة احتياطية

⚠️ **لا تستخدم على أنظمة دون إذن** - للأغراض التعليمية فقط!

---

## حل المشاكل الشائعة

### المشكلة: "لم يتم العثور على مفتاح API"

**الحل:**

```bash
export SHODAN_API_KEY="your_key_here"
python3 spear.py
```

### المشكلة: "ModuleNotFoundError"

**الحل:**

```bash
pip3 install -r requirements.txt
```

### المشكلة: "Connection timeout"

**الحل:** عدّل `config.py`:

```python
CONNECTION_TIMEOUT = 10  # زد المهلة
MAX_THREADS = 20  # قلل عدد الخيوط
```

---

## للاستخدام على Termux (Android)

```bash
# تثبيت المتطلبات
pkg update && pkg upgrade
pkg install python git

# استنساخ المشروع
cd ~
git clone <repo_url> shodans_spear
cd shodans_spear

# التثبيت
pip install -r requirements.txt

# التشغيل
python spear.py
```

---

## الأوامر الأساسية

| الأمر | الوصف |
|------|-------|
| `python3 spear.py` | تشغيل الأداة |
| `[1]` | صيد سريع |
| `[2]` | صيد شامل |
| `[4]` | مهاجمة الأهداف |
| `[5]` | عرض الغنائم |
| `[6]` | الإحصائيات |
| `[0]` | الخروج |

---

## موارد إضافية

- **الدليل الكامل**: اقرأ `README.md`
- **التكوين المتقدم**: عدّل `config.py`
- **Shodan Docs**: https://developer.shodan.io/

---

<div align="center">

**ᚺᚾᛉᚲᛏ رُمْح شودان**

*"أنا العاصفة التي تضرب من الظل"*

</div>
